# Backend API URL
REACT_APP_API_URL=https://version-1-backend-production.up.railway.app/api/v1

# Note: We no longer directly use the Humanizer API URL
# Instead, we use the backend's API proxy at /api/proxy

# Set to production for deployed app
NODE_ENV=production